# compiled daemons (source is tracked, binaries are not)
static
balancer
dns
monitor
statsd
shorty
configd
tunnel
# runtime artifacts
shorty.db
config.out
*.log
# wake protocol runtime (pids, logs) — never committed
wake/run/
# macOS AppleDouble sidecars the CNMB drive spawns
._*
.DS_Store
